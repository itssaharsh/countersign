# Mission

Win The Agent Harness Hackathon. Primary track: **Best UI** (user priority) while remaining a top
**Best Use of TrueForge** contender. Judged on 6 equal criteria: Impact, Originality, Technical
Excellence, Sponsor-Tool Use, Control & Safety, Presentation.

## The one-sentence pitch
Countersign refuses to render an Approve button until the agent has measured exactly what a
destructive change destroys and proven the rollback restores it.

## Qualification bar (from the event page, verbatim)
"A judge has to see TrueForge reaching a tool, running code in the sandbox, and stopping for a person."
→ github/supabase MCP reached ✓ · sandbox policy run (Daytona pending user) · commit_change pause ✓

## Cut list — apply IN THIS ORDER when behind schedule (triggers in parentheses)
1. exa CVE enrichment (any time pressure)
2. Adversarial countersignature second-model agent (Day 2 evening not started)
3. Subagent parallel lanes → sequential single lane (flaky in testing)
4. Generative UI inside stock chat (we have our own console — skip entirely if console works)
5. GitHub receipt-comment → screenshot in README only (Day 3 morning not working)
6. Landing page → README hero image only (Day 3 noon)
NEVER cut: shadow simulate + measured blast radius, verified undo, gated commit_change with
server-side refusal, drift detection, the console's three phases, replay mode, demo video.

## Conditional promotions
- If Supabase MCP connector cannot do what we need read-only → all measurement goes through our
  own server tools (already the default design — no risk).
- If Daytona never lands → policy engine runs in-server (dual path); demo says "sandbox-ready,
  shown here in server mode" — honest, and the approval pause still qualifies.
